#Find length of a list without using len ()

#Descripton 
<br>
In this program, we find the length of a python list without using thw built-in len() function. 
we use a loop to count each element and store the total count as the length of the list.
